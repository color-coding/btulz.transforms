package ${GroupId}.${ArtifactId}.${Domain.getName().toLowerCase()};

/**
 * 常量（命名空间等）
 */
public final class MyConsts {
    /**
    * 模块标识
    */
    public static final String MY_MODULE_ID = "${ProjectId}";
    /**
    * 命名空间
    */
    public static final String NAMESPACE_ROOT = "${ProjectUrl}/${ArtifactId}/${Domain.getName().toLowerCase()}/";
    /**
    * 数据命名空间
    */
    public static final String NAMESPACE_DATA = "${ProjectUrl}/${ArtifactId}/${Domain.getName().toLowerCase()}/data";
    /**
    * 业务对象命名空间
    */
    public static final String NAMESPACE_BO = "${ProjectUrl}/${ArtifactId}/${Domain.getName().toLowerCase()}/bo";
    /**
    * 服务命名空间
    */
    public static final String NAMESPACE_SERVICE = "${ProjectUrl}/${ArtifactId}/${Domain.getName().toLowerCase()}/service";

}

