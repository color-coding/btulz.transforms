package org.colorcoding.tools.btulz.shell.gui.commands;

import org.colorcoding.tools.btulz.shell.commands.CommandBuilder;

public interface BuilderTabListener {

	void builderSelected(CommandBuilder builder);
}
